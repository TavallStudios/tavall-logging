version: 2

source:
  repository: TavallStudios/tavall-logging
  provider: github

build:
  type: gradle
  java: 25
  gradle: 9.6.1

resources:
  mode: SYSTEM

artifacts:
  - name: tavall-logging
    path: build/tavall-ci-artifacts/tavall-logging.jar

requiredChecks:
  - quality
  - artifact

checks:
  quality:
    executor: gradle
    tasks:
      - "clean"
      - "check"
      - "assemble"

  artifact:
    executor: gradle
    tasks:
      - tavallCiArtifact
